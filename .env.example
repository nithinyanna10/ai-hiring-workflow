# Use a dedicated database name on your Postgres instance (avoids Supabase `public` + `auth` FK conflicts with Prisma push).
DATABASE_URL="postgresql://postgres:postgres@localhost:54322/hiring_workflow"
DIRECT_URL="postgresql://postgres:postgres@localhost:54322/hiring_workflow"
# Must match dev server port (see apps/web package.json `dev` script; default 4730 avoids 3000/5000/8080 clashes)
NEXT_PUBLIC_APP_URL="http://localhost:4730"
# Omit EMAIL_PROVIDER or set "resend" when RESEND_API_KEY is set (otherwise mail only logs to the server console).
# EMAIL_PROVIDER="mock"
# Verify this address (or your domain) in the Resend dashboard so mail can send.
EMAIL_FROM="nithinyanna3@gmail.com"
RESEND_API_KEY=""
OPENAI_API_KEY=""
OPENAI_MODEL="gpt-5.2"
SCREENING_SCORE_THRESHOLD="75"
SLACK_PROVIDER="mock"
SLACK_BOT_TOKEN=""
SLACK_TEAM_ID=""
SLACK_HR_CHANNEL_ID=""
SLACK_WELCOME_CHANNEL_ID=""
